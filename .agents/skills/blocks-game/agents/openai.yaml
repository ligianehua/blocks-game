interface:
  display_name: "Blocks Game"
  short_description: "Repo-specific patterns and workflows for blocks-game"
  default_prompt: "Use the blocks-game repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true